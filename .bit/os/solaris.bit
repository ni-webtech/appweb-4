/*
    solaris.bit -- Solaris Settings
 */

Bit.load({
    blend: [    
        'posix.bit',
        'gcc.bit',
    ],
    defaults: {
        '+libraries': [
            'lxnet', 
            'rt', 
            'socket',
        ],
        '+linker': [
            '-L${dir.lib}',
        ],
    },
})

