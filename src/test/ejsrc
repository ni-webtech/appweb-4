//  Ejsript default configuration
{
    mode: "debug",
    app: {
        cache: true,
        reload: true,
    },
    dirs: {
        cache: "cache",
    },
    log: {
        enable: true,
        location: "stderr",
        level: 2,
        showClient: true,
    },
    web: {
        limits: {
            inactivityTimeout: 3600,
            requestTimeout: 3600,
            sessionTimeout: 3600,
        },
        listen: "127.0.0.1:4000",
    },
}
