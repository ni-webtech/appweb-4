#
#   gcc.conf -- Embedded Server Pages configuration for GCC compilers
#

EspCompile cc -shared ${DEBUG} -Wall -Wno-unused-result -DPIC -fPIC -mtune=${ARCH} -I${INC} -L${LIB} -Wl,--enable-new-dtags -Wl,-rpath,$ORIGIN/ -Wl,-rpath,$ORIGIN/../lib ${LIB}/mod_esp${SHLIB} -lappweb -o ${OUT}${SHLIB} ${SRC}.c 
